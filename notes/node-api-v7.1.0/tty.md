# tty

*   Methods:

    ```
    tty.isatty(fd)
    ```

*   ReadStream

    ```
    tty.ReadStream

    readStream.isRaw
    readStream.setRawMode(mode)
    ```

*   WriteStream

    ```
    tty.WriteStream

    Events
      resize

    writeStream.columns
    writeStream.rows
    ```

