# util

```
util.debuglog(section)                              调试输出
util.deprecate(function, string)                    标记函数被弃用
util.format(format[, ...args])                      格式化字符串
util.inherits(constructor, superConstructor)        继承
util.inspect(object[, options])                     返回一个对象的字符串表现形式
```

